---
# ==============================================================================
# PLAY 1 : Nettoyage et Bypass Sécurité GPG 2026
# ==============================================================================
- name: "Nettoyage et Préparation"
  hosts: all
  become: yes
  tasks:
    - name: "Reset complet"
      shell: |
        kubeadm reset -f || true
        systemctl stop kubelet containerd || true
        rm -rf /etc/kubernetes /var/lib/kubelet /etc/containerd /etc/apt/sources.list.d/kubernetes.list
      ignore_errors: yes

    - name: "Configuration Système"
      shell: |
        modprobe overlay && modprobe br_netfilter
        cat <<EOF > /etc/sysctl.d/k8s.conf
        net.bridge.bridge-nf-call-iptables  = 1
        net.bridge.bridge-nf-call-ip6tables = 1
        net.ipv4.ip_forward                 = 1
        EOF
        sysctl --system
        swapoff -a

    - name: "Installation Containerd et Outils GPG"
      apt:
        name: [containerd, curl, gnupg, apt-transport-https, ca-certificates]
        state: present
        update_cache: yes

    - name: "FORCE : Configuration Containerd (Systemd + Sandbox 3.10)"
      shell: |
        mkdir -p /etc/containerd
        containerd config default > /etc/containerd/config.toml
        sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml
        sed -i 's|sandbox_image = .*|sandbox_image = "registry.k8s.io/pause:3.10"|' /etc/containerd/config.toml
        systemctl restart containerd

    - name: "Ajout du dépôt avec TRUSTED=YES (Bypass Signature v3 2026)"
      shell: |
        mkdir -p -m 755 /etc/apt/keyrings
        curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.31/deb/Release.key | gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg --yes
        echo "deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg trusted=yes] https://pkgs.k8s.io/core:/stable:/v1.31/deb/ /" > /etc/apt/sources.list.d/kubernetes.list

    - name: "Installation Kubelet, Kubeadm, Kubectl (Force)"
      apt:
        name: [kubelet, kubeadm, kubectl]
        state: present
        update_cache: yes
        allow_unauthenticated: yes

    - name: "Hold des versions"
      shell: apt-mark hold kubelet kubeadm kubectl

# ==============================================================================
# PLAY 2 : Initialisation Master (Correction Timeout API)
# ==============================================================================
- name: "Initialisation Master"
  hosts: master
  become: yes
  tasks:
    - name: "Correction Kubelet Config"
      shell: |
        mkdir -p /var/lib/kubelet
        cat <<EOF > /var/lib/kubelet/config.yaml
        apiVersion: kubelet.config.k8s.io/v1beta1
        kind: KubeletConfiguration
        cgroupDriver: systemd
        EOF

    - name: "Kubeadm Init"
      command: >
        kubeadm init 
        --pod-network-cidr=10.244.0.0/16 
        --apiserver-advertise-address={{ ansible_host }}
        --cri-socket=unix:///var/run/containerd/containerd.sock
        --ignore-preflight-errors=all
      register: kubeadm_init

    - name: "Setup Kubeconfig"
      shell: |
        mkdir -p /home/ubuntu/.kube
        cp /etc/kubernetes/admin.conf /home/ubuntu/.kube/config
        chown ubuntu:ubuntu /home/ubuntu/.kube/config
        sudo -u ubuntu kubectl apply -f https://github.com/flannel-io/flannel/releases/latest/download/kube-flannel.yml

    - name: "Token de jonction"
      shell: kubeadm token create --print-join-command
      register: join_cmd

    - name: "Propagation du Token"
      set_fact:
        final_join_command: "{{ join_cmd.stdout }}"
      delegate_to: "{{ item }}"
      delegate_facts: true
      with_items: "{{ groups['workers'] }}"

# ==============================================================================
# PLAY 3 : Jonction Workers et Validation
# ==============================================================================
- name: "Jonction Workers"
  hosts: workers
  become: yes
  tasks:
    - name: "Join Cluster"
      command: "{{ final_join_command }} --ignore-preflight-errors=all"

- name: "Vérification Finale"
  hosts: master
  become: yes
  tasks:
    - name: "Vérifier les nœuds"
      shell: "kubectl get nodes"
      register: res
      until: 'res.stdout_lines | length >= 4'
      retries: 15
      delay: 10
    - debug:
        msg: "{{ res.stdout_lines }}"
