---
# ==============================================================================
# PLAY 0 : Reset Total (Nettoyage agressif pour réinstallation propre)
# ==============================================================================
- name: Reset complet du cluster
  hosts: all
  become: yes
  tasks:
    - name: Nettoyage kubeadm et répertoires
      shell: |
        kubeadm reset -f || true
        systemctl stop kubelet || true
        systemctl stop containerd || true
        ip link delete cni0 || true
        ip link delete flannel.1 || true
        rm -rf /etc/cni/net.d /var/lib/cni/ /var/lib/kubelet/* /var/lib/etcd /etc/kubernetes/* /home/ubuntu/.kube
      failed_when: false

# ==============================================================================
# PLAY 1 : Configuration Système & Registre Local
# ==============================================================================
- name: Configuration Système
  hosts: all
  become: yes
  tasks:
    - name: Hostname et Correction /etc/hosts
      shell: |
        hostnamectl set-hostname {{ inventory_hostname }}
        sed -i '/127.0.1.1/d' /etc/hosts
        echo "127.0.0.1 {{ inventory_hostname }}" >> /etc/hosts
      args:
        executable: /bin/bash

    - name: Correction lien DNS pour Pod Sandbox
      shell: |
        mkdir -p /run/systemd/resolve
        ln -sf /etc/resolv.conf /run/systemd/resolve/resolv.conf
      args:
        executable: /bin/bash

    - name: Configurer Containerd avec support Registre Local
      shell: |
        modprobe overlay && modprobe br_netfilter
        mkdir -p /etc/containerd
        containerd config default > /etc/containerd/config.toml
        # Activation SystemdCgroup
        sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml
        # Ajout du registre local insecure (Port 5000 sur le Master)
        sed -i '/\[plugins."io.containerd.grpc.v1.cri".registry.mirrors\]/a \        [plugins."io.containerd.grpc.v1.cri".registry.mirrors."152.228.130.229:5000"]\n          endpoint = ["http://152.228.130.229:5000"]' /etc/containerd/config.toml
        systemctl restart containerd
        systemctl enable --now kubelet
      args:
        executable: /bin/bash

# ==============================================================================
# PLAY 2 : Initialisation du Master (Dynamique)
# ==============================================================================
- name: Initialisation du Master
  hosts: master
  become: yes
  tasks:
    - name: Kubeadm Init (Sans token statique)
      command: kubeadm init --pod-network-cidr=10.244.0.0/16 --ignore-preflight-errors=NumCPU --node-name {{ inventory_hostname }}
      register: kubeadm_init

    - name: Créer le répertoire .kube pour l'utilisateur ubuntu
      file:
        path: /home/ubuntu/.kube
        state: directory
        mode: '0755'
        owner: ubuntu
        group: ubuntu

    - name: Copier la config admin
      copy:
        src: /etc/kubernetes/admin.conf
        dest: /home/ubuntu/.kube/config
        remote_src: yes
        owner: ubuntu
        group: ubuntu

    - name: Installer Flannel (Réseau)
      become_user: ubuntu
      command: kubectl apply -f https://github.com/flannel-io/flannel/releases/latest/download/kube-flannel.yml

    - name: Générer la commande de join pour les workers
      command: kubeadm token create --print-join-command
      register: join_command_raw

    - name: Sauvegarder la commande de join dans un fait global
      set_fact:
        join_command: "{{ join_command_raw.stdout }}"

# ==============================================================================
# PLAY 3 : Jonction des Workers
# ==============================================================================
- name: Jonction Workers
  hosts: workers
  become: yes
  tasks:
    - name: Rejoindre le cluster avec la commande dynamique
      command: "{{ hostvars[groups['master'][0]]['join_command'] }} --node-name {{ inventory_hostname }}"
      register: worker_join
      until: worker_join.rc == 0
      retries: 3
      delay: 10

# ==============================================================================
# PLAY 4 : Validation et Santé du Cluster
# ==============================================================================
- name: Validation
  hosts: master
  become: yes
  tasks:
    - name: Attendre que les 3 noeuds soient Ready
      shell: "kubectl --kubeconfig=/etc/kubernetes/admin.conf get nodes | grep ' Ready ' | wc -l"
      register: nodes_ready
      until: nodes_ready.stdout|int == 3
      retries: 20
      delay: 15
      changed_when: false

    - name: Affichage final des noeuds
      command: "kubectl --kubeconfig=/etc/kubernetes/admin.conf get nodes"
      register: final_status
    
    - debug:
        msg: "{{ final_status.stdout_lines }}"
