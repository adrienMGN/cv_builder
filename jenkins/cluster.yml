---
# ==============================================================================
# PLAY 1 : Configuration Système K8s
# ==============================================================================
- name: "Configuration Système K8s"
  hosts: all
  become: yes
  tasks:
    - name: "Hostname et Correction /etc/hosts"
      shell: |
        hostnamectl set-hostname {{ inventory_hostname }}
        sed -i '/127.0.1.1/d' /etc/hosts
        echo "127.0.0.1 {{ inventory_hostname }}" >> /etc/hosts
      args:
        executable: /bin/bash

    - name: "Désactiver le Swap"
      shell: |
        swapoff -a
        sed -i '/ swap / s/^\(.*\)$/#\1/g' /etc/fstab
      args:
        executable: /bin/bash

    - name: "Nettoyage des verrous APT"
      shell: |
        rm -f /var/lib/apt/lists/lock
        rm -f /var/cache/apt/archives/lock
        rm -f /var/lib/dpkg/lock*
      ignore_errors: yes

    - name: "Installation des pré-requis"
      apt:
        name: [apt-transport-https, ca-certificates, curl, gnupg, conntrack, socat, containerd]
        state: present
        update_cache: yes
      ignore_errors: yes

    - name: "Configurer les modules Kernel"
      copy:
        dest: /etc/modules-load.d/k8s.conf
        content: |
          overlay
          br_netfilter

    - name: "Appliquer les modules Kernel"
      shell: modprobe overlay && modprobe br_netfilter

    - name: "Configurer Sysctl"
      copy:
        dest: /etc/sysctl.d/k8s.conf
        content: |
          net.bridge.bridge-nf-call-iptables  = 1
          net.bridge.bridge-nf-call-ip6tables = 1
          net.ipv4.ip_forward                 = 1

    - name: "Appliquer Sysctl"
      command: sysctl --system

    - name: "Configuration de Containerd"
      shell: |
        mkdir -p /etc/containerd
        containerd config default > /etc/containerd/config.toml
        sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml
        systemctl restart containerd
      args:
        executable: /bin/bash

    - name: "Nettoyage radical des anciens dépôts"
      file:
        path: "{{ item }}"
        state: absent
      with_items:
        - /etc/apt/sources.list.d/kubernetes.list
        - /etc/apt/keyrings/kubernetes-archive-keyring.gpg

    - name: "Créer le dossier keyrings"
      file:
        path: /etc/apt/keyrings
        state: directory
        mode: '0755'

    - name: "Télécharger la clé GPG Kubernetes"
      get_url:
        url: https://pkgs.k8s.io/core:/stable:/v1.31/deb/Release.key
        dest: /tmp/k8s-release.key

    - name: "Désarchiver la clé GPG"
      shell: cat /tmp/k8s-release.key | gpg --dearmor --yes -o /etc/apt/keyrings/kubernetes-archive-keyring.gpg

    - name: "Ajouter le dépôt Kubernetes v1.31 (Fix Force 2026)"
      copy:
        dest: /etc/apt/sources.list.d/kubernetes.list
        # Utilisation de [trusted=yes] ET [signed-by] pour bypasser les erreurs de signature v3 en 2026
        content: "deb [signed-by=/etc/apt/keyrings/kubernetes-archive-keyring.gpg trusted=yes] https://pkgs.k8s.io/core:/stable:/v1.31/deb/ /"

    - name: "Installer Kubeadm, Kubelet et Kubectl"
      shell: |
        apt-mark unhold kubelet kubeadm kubectl || true
        apt-get update -o Dir::Etc::sourcelist="sources.list.d/kubernetes.list" -o Dir::Etc::sourceparts="-" -o APT::Get::List-Cleanup="0"
        apt-get install -y --allow-unauthenticated --allow-change-held-packages kubelet kubeadm kubectl
        apt-mark hold kubelet kubeadm kubectl
      args:
        executable: /bin/bash

# ==============================================================================
# PLAY 2 : Initialisation du Master
# ==============================================================================
- name: "Initialisation du Master"
  hosts: master
  become: yes
  tasks:
    - name: "Initialiser le cluster avec Kubeadm"
      command: >
        kubeadm init 
        --pod-network-cidr=10.244.0.0/16 
        --node-name {{ inventory_hostname }}
        --apiserver-advertise-address={{ ansible_host }}
        --kubernetes-version=v1.31.0
      register: kubeadm_init
      failed_when: "'already exists' not in kubeadm_init.stderr and kubeadm_init.rc != 0"

    - name: "Configurer Kubeconfig"
      file:
        path: "/home/ubuntu/.kube"
        state: directory
        owner: ubuntu
        group: ubuntu

    - name: "Copier admin.conf"
      copy:
        src: /etc/kubernetes/admin.conf
        dest: "/home/ubuntu/.kube/config"
        remote_src: yes
        owner: ubuntu
        group: ubuntu

    - name: "Attendre que le Control Plane soit stable"
      pause:
        seconds: 30

    - name: "Installer le plugin réseau Flannel"
      become_user: ubuntu
      command: kubectl apply -f https://github.com/flannel-io/flannel/releases/latest/download/kube-flannel.yml
      register: flannel_res
      until: flannel_res.rc == 0
      retries: 5
      delay: 10

    - name: "Générer la commande de join"
      command: kubeadm --kubeconfig=/etc/kubernetes/admin.conf token create --print-join-command
      register: join_cmd

    - name: "Partager la commande de join"
      set_fact:
        join_command: "{{ join_cmd.stdout }}"
      delegate_to: "{{ item }}"
      delegate_facts: true
      with_items: "{{ groups['workers'] }}"

# ==============================================================================
# PLAY 3 : Jonction des Workers
# ==============================================================================
- name: "Jonction des Workers"
  hosts: workers
  become: yes
  tasks:
    - name: "Rejoindre le cluster"
      command: "{{ join_command }} --node-name {{ inventory_hostname }} --ignore-preflight-errors=all"
      register: joined
      failed_when: "'already exists' not in joined.stderr and joined.rc != 0"

# ==============================================================================
# PLAY 4 : Validation
# ==============================================================================
- name: "Validation du Cluster"
  hosts: master
  become: yes
  tasks:
    - name: "Attendre que les nœuds soient Ready"
      shell: "kubectl --kubeconfig=/etc/kubernetes/admin.conf get nodes | grep ' Ready ' | wc -l"
      register: ready_count
      # On attend que le Master + les 2 Workers soient Ready (total = 3)
      until: ready_count.stdout|int >= 3
      retries: 20
      delay: 15

    - name: "Afficher l'état final"
      command: "kubectl --kubeconfig=/etc/kubernetes/admin.conf get nodes"
      register: final_res
    - debug:
        msg: "{{ final_res.stdout_lines }}"
