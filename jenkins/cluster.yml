---
- name: "Configuration Kubernetes - Harmonisation Totale"
  hosts: all
  become: yes
  tasks:
    - name: "Hostname et Correction /etc/hosts"
      shell: |
        hostnamectl set-hostname {{ inventory_hostname }}
        sed -i '/127.0.1.1/d' /etc/hosts
        echo "127.0.0.1 {{ inventory_hostname }}" >> /etc/hosts

    - name: "Configuration Réseau Noyau"
      shell: |
        modprobe overlay
        modprobe br_netfilter
        cat <<EOF > /etc/sysctl.d/k8s.conf
        net.bridge.bridge-nf-call-iptables  = 1
        net.bridge.bridge-nf-call-ip6tables = 1
        net.ipv4.ip_forward                 = 1
        EOF
        sysctl --system

    - name: "Désactiver le Swap"
      shell: |
        swapoff -a
        sed -i '/ swap / s/^\(.*\)$/#\1/g' /etc/fstab

    - name: "Installation Containerd et outils"
      apt:
        name: [containerd, socat, conntrack, iptables, curl]
        state: present
        update_cache: yes

    - name: "CONFIGURATION CRUCIALE : Containerd & Sandbox"
      shell: |
        mkdir -p /etc/containerd
        containerd config default > /etc/containerd/config.toml
        # Activation du Cgroup Systemd
        sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml
        # Correction de l'image sandbox (pause)
        sed -i 's|sandbox_image = .*|sandbox_image = "registry.k8s.io/pause:3.10"|' /etc/containerd/config.toml
        systemctl restart containerd

    - name: "Configuration de crictl"
      copy:
        dest: /etc/crictl.yaml
        content: |
          runtime-endpoint: unix:///var/run/containerd/containerd.sock
          image-endpoint: unix:///var/run/containerd/containerd.sock
          timeout: 10
          debug: false

    - name: "Installation des Binaires K8s"
      shell: |
        mkdir -p /opt/cni/bin
        curl -L "https://github.com/containernetworking/plugins/releases/download/v1.3.0/cni-plugins-linux-amd64-v1.3.0.tgz" | tar -C /opt/cni/bin -xz
        cd /usr/bin
        curl -L -o kubeadm https://dl.k8s.io/release/v1.31.0/bin/linux/amd64/kubeadm
        curl -L -o kubelet https://dl.k8s.io/release/v1.31.0/bin/linux/amd64/kubelet
        curl -L -o kubectl https://dl.k8s.io/release/v1.31.0/bin/linux/amd64/kubectl
        chmod +x kubeadm kubelet kubectl

    - name: "Service Kubelet Minimal"
      copy:
        dest: /etc/systemd/system/kubelet.service
        content: |
          [Unit]
          Description=kubelet: The Kubernetes Node Agent
          [Service]
          ExecStart=/usr/bin/kubelet
          Restart=always
          [Install]
          WantedBy=multi-user.target

    - name: "Configuration Kubelet (Systemd Driver Force)"
      shell: |
        mkdir -p /var/lib/kubelet
        cat <<EOF > /var/lib/kubelet/config.yaml
        apiVersion: kubelet.config.k8s.io/v1beta1
        kind: KubeletConfiguration
        cgroupDriver: systemd
        EOF
        mkdir -p /etc/systemd/system/kubelet.service.d
        echo -e '[Service]\nExecStart=\nExecStart=/usr/bin/kubelet --config=/var/lib/kubelet/config.yaml --container-runtime-endpoint=unix:///var/run/containerd/containerd.sock' > /etc/systemd/system/kubelet.service.d/10-kubeadm.conf
        systemctl daemon-reload
        systemctl enable --now kubelet

# ==============================================================================
# PLAY 2 : Initialisation du Master
# ==============================================================================
- name: "Initialisation du Master"
  hosts: master
  become: yes
  tasks:
    - name: "Kubeadm Reset"
      command: /usr/bin/kubeadm reset -f

    - name: "Pre-pull images"
      command: /usr/bin/kubeadm config images pull

    - name: "Initialiser le cluster"
      command: >
        /usr/bin/kubeadm init 
        --pod-network-cidr=10.244.0.0/16 
        --apiserver-advertise-address={{ ansible_host }}
        --ignore-preflight-errors=all
      register: kubeadm_init

    - name: "Configurer Kubeconfig"
      shell: |
        mkdir -p /home/ubuntu/.kube
        cp /etc/kubernetes/admin.conf /home/ubuntu/.kube/config
        chown ubuntu:ubuntu /home/ubuntu/.kube/config

    - name: "Installer Flannel"
      become_user: ubuntu
      command: /usr/bin/kubectl apply -f https://github.com/flannel-io/flannel/releases/latest/download/kube-flannel.yml

    - name: "Générer join command"
      shell: "/usr/bin/kubeadm token create --print-join-command"
      register: join_cmd

    - name: "Partager join command"
      set_fact:
        final_join_command: "{{ join_cmd.stdout }}"
      delegate_to: "{{ item }}"
      delegate_facts: true
      with_items: "{{ groups['workers'] }}"

# ==============================================================================
# PLAY 3 : Jonction des Workers
# ==============================================================================
- name: "Jonction des Workers"
  hosts: workers
  become: yes
  tasks:
    - name: "Kubeadm Reset Worker"
      command: /usr/bin/kubeadm reset -f

    - name: "Rejoindre le cluster"
      command: "{{ final_join_command }} --ignore-preflight-errors=all"
