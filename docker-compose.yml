version: '3.8'

services:
  # Frontend
  cv-builder:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # ON CHANGE ICI : On pointe vers le domaine HTTPS, chemin /api
        - VITE_API_URL=https://cv-builder.skirshta.fr
    ports:
      - "3000:80"
    container_name: cv-builder-app
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      # Idem ici pour être sûr
      - VITE_API_URL=https://cv-builder.skirshta.fr/api
    logging:
      driver: "json-file"
      options:
        max-size: "2m"
        max-file: "3"

  # Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5001:5000"
    container_name: cv-builder-backend
    restart: unless-stopped
    depends_on:
      - mongo
    environment:
      - PORT=5000
      - MONGODB_URI=mongodb://mongo:27017/cvbuilder
      - JWT_SECRET=change-this-super-secret-key-in-production-123456789
      - NODE_ENV=production
      - CV_LIFETIME_MINUTES=10
      # AJOUT ICI : On ajoute le nouveau domaine HTTPS dans la liste
      - ALLOWED_ORIGINS=http://vps-82ecffcd.vps.ovh.net:3000,http://localhost:3000,https://cv-builder.skirshta.fr
      - RESEND_API_KEY=re_eS1MaEPr_QKcnREpze4LXQZx9FfxT4q9B
    logging:
      driver: "json-file"
      options:
        max-size: "2m"
        max-file: "3"

  # MongoDB
  mongo:
    image: mongo:7
    container_name: cv-builder-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=cvbuilder
    logging:
      driver: "json-file"
      options:
        max-size: "1m"
        max-file: "2"

  # Mongo Express - Interface Web MongoDB
  mongo-express:
    image: mongo-express:latest
    container_name: cv-builder-mongo-express
    restart: unless-stopped
    ports:
      - "8081:8081"
    environment:
      - ME_CONFIG_MONGODB_URL=mongodb://mongo:27017
      - ME_CONFIG_MONGODB_ADMINUSERNAME=
      - ME_CONFIG_MONGODB_ADMINPASSWORD=
      - ME_CONFIG_BASICAUTH_USERNAME=admin
      - ME_CONFIG_BASICAUTH_PASSWORD=admin123
    depends_on:
      - mongo
    logging:
      driver: "json-file"
      options:
        max-size: "1m"
        max-file: "2"

volumes:
  mongo-data:
