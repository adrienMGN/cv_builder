---
# ==============================================================================
# PLAY 0, 1, 2 : Reset et Préparation (Inchangés)
# ==============================================================================
- name: Reset et Préparation
  hosts: all
  become: yes
  tasks:
    - name: Reset kubeadm
      shell: kubeadm reset -f || true
      failed_when: false
    - name: Nettoyage dossiers
      file:
        path: "{{ item }}"
        state: absent
      loop: [/etc/cni/net.d, /var/lib/etcd, /etc/kubernetes, /home/ubuntu/.kube]

- name: Configuration Système
  hosts: all
  become: yes
  tasks:
    - name: Hostname et Containerd
      shell: |
        hostnamectl set-hostname {{ inventory_hostname }}
        modprobe overlay && modprobe br_netfilter
        systemctl restart containerd

# ==============================================================================
# PLAY 3 : Master - On récupère le Hash CA dynamiquement
# ==============================================================================
- name: Initialisation du Master
  hosts: master
  become: yes
  tasks:
    - name: Kubeadm Init
      command: kubeadm init --pod-network-cidr=10.244.0.0/16 --token abcdef.1234567890abcdef --ignore-preflight-errors=NumCPU --node-name {{ inventory_hostname }}

    - name: Calculer le CA Hash dynamiquement
      shell: openssl x509 -pubkey -in /etc/kubernetes/pki/ca.crt | openssl rsa -pubin -outform der 2>/dev/null | openssl dgst -sha256 -hex | sed 's/^.* //'
      register: dynamic_ca_hash

    - name: Installer Flannel et Configurer Kubeconfig
      shell: |
        mkdir -p /home/ubuntu/.kube
        cp -f /etc/kubernetes/admin.conf /home/ubuntu/.kube/config
        chown -R ubuntu:ubuntu /home/ubuntu/.kube
        kubectl --kubeconfig=/etc/kubernetes/admin.conf apply -f https://github.com/flannel-io/flannel/releases/latest/download/kube-flannel.yml
      register: flannel_res
      until: flannel_res.rc == 0
      retries: 5
      delay: 15

# ==============================================================================
# PLAY 4 : Workers - Jonction dynamique
# ==============================================================================
- name: Jonction Workers
  hosts: workers
  become: yes
  tasks:
    - name: Pause de sécurité pour le Master
      pause:
        seconds: 20

    - name: Rejoindre le cluster
      command: >
        kubeadm join 152.228.130.229:6443 
        --token abcdef.1234567890abcdef 
        --discovery-token-ca-cert-hash sha256:{{ hostvars[groups['master'][0]]['dynamic_ca_hash']['stdout'] }}
        --node-name {{ inventory_hostname }}
      register: join_res
      until: join_res.rc == 0
      retries: 5
      delay: 20

# ==============================================================================
# PLAY 5 : Validation Finale (Version Robuste)
# ==============================================================================
- name: Validation
  hosts: master
  become: yes
  tasks:
    - name: Attendre que les noeuds passent Ready
      # On utilise --kubeconfig pour éviter l'erreur TLS x509
      shell: "kubectl --kubeconfig=/etc/kubernetes/admin.conf get nodes | grep ' Ready ' | wc -l"
      register: nodes_ready
      until: nodes_ready.stdout|int == 3
      retries: 20
      delay: 15
      changed_when: false

    - name: Statut final (Affichage)
      command: "kubectl --kubeconfig=/etc/kubernetes/admin.conf get nodes"
      register: res
    - debug:
        var: res.stdout_lines
